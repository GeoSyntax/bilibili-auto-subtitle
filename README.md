# bilibili-auto-subtitle

一个用于 B 站视频页的 Tampermonkey 油猴脚本：当检测到当前视频存在 AI 字幕时，自动开启字幕，并提供可视化开关、快捷键、自定义快捷键、面板隐藏与提示气泡等功能。

## 功能特性

- 自动检测当前视频是否存在 AI 字幕
- 检测到后自动开启 AI 字幕
- 支持 B 站 SPA 路由切换
- 支持多 P / `cid` 变化重新判断
- 右下角悬浮控制面板
- 支持开启 / 关闭自动功能
- 支持隐藏 / 显示控制面板
- 支持提示气泡自动淡出
- 支持自定义快捷键
- 记住自动功能开关状态
- 记住面板显示 / 隐藏状态
- 用户手动关闭字幕后，不会持续抢控制权

## 文件说明

- `bilibili-auto-ai-subtitle.user.js`：油猴脚本主文件

## 安装方法

### 方法一：本地导入（推荐）

1. 先安装浏览器扩展：
   - Tampermonkey
2. 打开 Tampermonkey 管理面板。
3. 选择“创建新脚本”或“导入”。
4. 将 `bilibili-auto-ai-subtitle.user.js` 文件内容完整粘贴进去。
5. 保存脚本。
6. 打开任意 B 站视频页进行测试。

### 方法二：从 GitHub 原始文件导入

上传到 GitHub 后，可以打开该脚本文件的 Raw 链接，Tampermonkey 通常会自动识别并提示安装。

## 油猴导入使用技巧

### 1. 推荐开启开发者模式 / 允许用户脚本运行
不同浏览器对脚本安装提示策略不同，如果没有自动弹出安装页：

- 先确认 Tampermonkey 已启用
- 刷新目标页面后重新打开脚本链接
- 检查扩展是否允许在当前站点运行

### 2. 修改脚本后要重新保存 / 重新安装
如果你手动修改了 `.user.js` 文件：

- 需要重新复制到 Tampermonkey 中保存
- 或重新打开新的 Raw 链接重新安装

### 3. B 站是单页应用，测试时建议验证以下场景

- 直接打开视频页
- 站内跳转到另一个视频
- 多 P 视频切换分 P
- 手动关闭后重新开启
- 面板隐藏后再恢复

### 4. 快捷键冲突时可用油猴菜单修改
在 Tampermonkey 脚本菜单中，可以设置：

- 自动开关快捷键
- 面板显隐快捷键

脚本会自动保存你的快捷键配置。

## 使用说明

### 面板功能
右下角会显示一个悬浮面板，例如：

- `AI字幕自动开：开（Alt+A）`
- `AI字幕自动开：关（Alt+A）`

点击主按钮可以切换自动功能。
点击 `×` 可以隐藏面板。

### 默认快捷键

- `Alt+A`：切换自动开字幕功能
- `Alt+H`：显示 / 隐藏控制面板

### 自定义快捷键
打开 Tampermonkey 脚本菜单，可以看到：

- 设置自动开关键
- 设置面板显隐键

支持输入例如：

- `Alt+A`
- `Shift+S`
- `Ctrl+Alt+Z`

### 提示气泡
进行以下操作时，会出现自动淡出的提示：

- 开启自动字幕
- 关闭自动字幕
- 隐藏面板
- 显示面板
- 修改快捷键

## 工作原理

脚本会优先通过结构化数据、接口与 DOM 组合方式检测 AI 字幕，并优先尝试选择 B 站当前播放器中的 AI 中文字幕项，例如：

- `div[data-lan="ai-zh"]`

在检测到存在 AI 字幕后，自动开启字幕。

## 常见问题

### 1. 为什么某些视频没有反应？
可能原因：

- 当前视频没有 AI 字幕
- B 站页面结构发生变化
- 当前字幕按钮 / 字幕项选择器失效
- 页面尚未加载完成

### 2. 为什么我手动关闭后脚本不立即重开？
这是刻意设计：

- 避免脚本和用户抢控制权
- 你可以通过面板或快捷键再次开启自动功能

### 3. 为什么隐藏面板后找不到了？
使用快捷键：

- 默认 `Alt+H`

如果你改过快捷键，请在 Tampermonkey 菜单里查看或重新设置。

## 截图占位

> 你可以后续在这里补充截图：

### 1. 脚本运行面板

<!-- 在这里插入面板截图 -->

### 2. 自动检测 AI 字幕效果

<!-- 在这里插入字幕效果截图 -->

### 3. Tampermonkey 菜单自定义快捷键

<!-- 在这里插入菜单截图 -->

## 开发与同步

如果你本地修改后想同步到 GitHub：

```bash
git status
git add README.md bilibili-auto-ai-subtitle.user.js .gitignore
git commit -m "Update userscript"
git push
```

## License

MIT
